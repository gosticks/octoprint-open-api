type: object
properties:
  current:
    type: object
    properties:
      state:
        type: string
      port:
        type: string
      baudrate:
        type: number
      printerProfile:
        type: string
  options:
    $ref: ./ConnectionOptions.yaml
